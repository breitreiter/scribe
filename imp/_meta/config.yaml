# Project substrate configuration. All keys optional; defaults shown.
# Uncomment and edit as needed.

# location: imp/                            # substrate location, relative to repo root
# proposals: ../scribe.imp-proposals/     # imp proposal sidecar (gitignored)
